import random

# 生成30个20到50之间的浮点数
numbers = [random.uniform(20, 50) for _ in range(30)]
# 统计大于30的数量
count_above_30 = sum(1 for num in numbers if num > 30)

# 输出生成的数值和大于30的数量
print("生成的浮点数:", numbers)
print("大于30的数量:", count_above_30)
